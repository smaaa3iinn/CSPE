# Work / integration scratch

This tree is **not** part of the CSPE Python package (`src.core`, `src.viz`, …).  
Use it to hold copies of other projects (e.g. Atlas) while you plan the monorepo and the “canal” between apps.

Do **not** add `__init__.py` here unless you intentionally want `import src.work`.
